@model PagedList.IPagedList<AgencyPackage>
@using PagedList.Mvc;
@using PagedList;
@{ var PageUtils = new PageUtils(); UserPage user = new UserPage();}
@foreach (var item in Model)
{
    <div class="control" data-id="@item.Id">
        <a href="/AgencyPackage/Edit/@item.Id" class="iframe_agencypackage_add">
            <span class="btn btn-xs btn-info edit-item">
                <i class="ace-icon fa fa-pencil bigger-120 "></i>
            </span>
        </a>
        <a href="#" class="delete-item-main" data-url="/AgencyPackage/Delete/@item.Id" data-id="tr_@item.Id">
            <span class="btn btn-xs btn-danger" data-url="/AgencyPackage/Delete/@item.Id" data-id="tr_@item.Id">
                <i class="ace-icon fa fa-trash-o bigger-120"></i>
            </span>
        </a>
    </div>
}
<script src="~/better-life/assets/js/jquery.2.1.1.min.js"></script>
<script src="~/better-life/assets/colorbox/jquery.colorbox.js"></script>
<style>
    #cboxClose {
        position: absolute;
        width: 25px;
        height: 25px;
        text-indent: -9999px;
        z-index: 99999;
        top: 12px !important;
        right: 12px !important;
    }
</style>
<script>
    $(function () {
        $(".iframe_agencypackage_add").colorbox({ iframe: true, innerWidth: 600, innerHeight: 670, overlayClose: false });
    })

    function paging(number) {
        $(".item-of-page").html("");
        $(".item-of-page").load('/Ajax/AgencyPackage?page=' + number + '&key=@(ViewBag.key)');
        $(".action-crud").html("");
        $(".action-crud").load('/Ajax/AgencyPackageControl?page=' + number + '&key=@(ViewBag.key)');
    }
</script>